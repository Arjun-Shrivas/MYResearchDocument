pythoncode mythril

psycopg2 error occure:  solution
sudo apt install libpq-dev  python3-dev
then pip3 install psycopg2
pip install py-solc-x

SolidityAnalyzeReporter().solidity_scan(self, solidity_file/suicide.sol)
 
solidity_scan(self, solidity_file/suicide.sol)
compilerVersion = self.getCompilerVersion(solidity_file)

